[build-system]
requires = [
    "setuptools",
    "wheel",
]
build-backend = "setuptools.build_meta"

[project]
name = "piperformance"
version = "0.1.0.dev0"
dependencies = [
    "tqdm",
]

[tool.setuptools]
py-modules = ["piperformance"]
